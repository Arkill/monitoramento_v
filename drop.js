<script>
        // Funções de ação para o dropdown
        function editClient(clientId) {
            alert('Editando cliente: ' + clientId);
        }

        function deleteClient(clientId) {
            alert('Excluindo cliente: ' + clientId);
        }

        function loginAsClient(clientId) {
            alert('Logando como cliente: ' + clientId);
        }
    </script>